name: Tests
on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: test
    uses: ./.github/workflows/setup.yml
    with:
      command: composer tests
